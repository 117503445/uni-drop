version: '3.9'
services:
  # bittorrent-tracker-dev:
  #   build:
  #     context: ./bittorrent-tracker
  #     dockerfile: Dockerfile_dev

  #   container_name: bittorrent-tracker-dev
  #   # restart: unless-stopped
  #   volumes:
  #       - './bittorrent-tracker:/workspace/bittorrent-tracker'
  #       - './vsc_extensions/bittorrent-tracker:/root/.vscode-server/extensions'
  #   stdin_open: true
  #   tty: true
  web-drop-dev:
    build:
      context: ./web-drop
      dockerfile: Dockerfile_dev

    container_name: web-drop-dev
    # restart: unless-stopped
    volumes:
        - './web-drop:/workspace/web-drop'
        - './dev-data/web-drop/vsc_extensions:/root/.vscode-server/extensions'
    stdin_open: true
    tty: true